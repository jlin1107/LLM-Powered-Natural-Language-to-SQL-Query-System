# LLM-Powered-Natural-Language-to-SQL-Query-System
Natural language-to-SQL application powered by the Phi-4 Mini Instruct LLM. Converts user questions into SQL queries, executes them on a SQLite database, and returns results through both a CLI and an interactive Streamlit web interface.
